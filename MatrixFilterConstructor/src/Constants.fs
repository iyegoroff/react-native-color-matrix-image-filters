namespace MatrixFilterConstructor

open Fable.Import.ReactNative

module Constants =
  let imageHeight = (Globals.Dimensions.get "screen").height * 0.5
  let commonFilterPortal = "commonFilterPortal"
  let animatedFilterPortal = "animatedFilterPortal"
  let imagePortal = "imagePortal"
