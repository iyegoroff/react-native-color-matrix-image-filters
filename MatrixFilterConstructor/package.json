{
  "name": "MatrixFilterConstructor",
  "version": "0.0.2",
  "private": true,
  "scripts": {
    "preinstall": "cd .. && npm i --only=prod",
    "postinstall": "metro-with-symlinks && npm run paket:install",
    "build": "fable-splitter -c splitter.config.js --define RELEASE",
    "watch": "fable-splitter -c splitter.config.js -w --define DEBUG",
    "run:android": "npm run clean:android && react-native run-android",
    "run:android:release": "npm run clean:android && npm run generate:android:bundle && react-native run-android --variant=release",
    "clean:android": "cd android && ./gradlew clean",
    "clean:ios": "cd ios && xcodebuild -target MatrixFilterConstructor -configuration Debug -sdk iphonesimulator clean && rm -rf build",
    "reset:packager": "watchman watch-del-all && node_modules/react-native/scripts/packager.sh --reset-cache",
    "start:packager": "react-native start",
    "restore": "cd src && dotnet restore",
    "start": "rm -rf out && cd src && dotnet fable npm-run watch",
    "paket:add": "mono .paket/paket.exe add",
    "paket:remove": "mono .paket/paket.exe remove",
    "paket:update": "mono .paket/paket.exe update",
    "paket:install": "mono .paket/paket.exe install && npm run restore",
    "build:release": "rm -rf out && cd src && dotnet fable npm-run build",
    "generate:android:signing-key": "keytool -genkey -v -keystore mfc.keystore -alias mfc -keyalg RSA -keysize 2048 -validity 10000 && mv mfc.keystore android/app",
    "generate:android:apk": "npm run generate:android:bundle && cd android && ./gradlew assembleRelease",
    "generate:android:bundle": "npm run build:release && react-native bundle --platform android --dev false --entry-file index.js --bundle-output ./android/app/src/main/assets/index.android.bundle --assets-dest ./android/app/src/main/res/",
    "build:docker:image": "docker build -t mfc-android.image -f ./Dockerfile ../",
    "extract:docker:apk": "docker create -ti --name mfc-android-container mfc-android.image /bin/bash && docker cp mfc-android-container:/package/MatrixFilterConstructor/android/app/build/outputs/apk/app-release.apk mfc.apk && docker rm -fv mfc-android-container",
    "build:release:docker": "npm run build:docker:image && npm run extract:docker:apk"
  },
  "dependencies": {
    "buffer": "5.1.0",
    "core-js": "2.5.7",
    "fast-memoize": "2.5.1",
    "react": "16.8.3",
    "react-native": "0.59.8",
    "react-native-color-matrix-image-filters": "file:../",
    "react-native-color-wheel": "0.1.5",
    "react-native-image-picker": "0.28.1",
    "react-native-portal": "1.3.0",
    "react-native-segmented-control-tab": "3.2.2",
    "react-native-slider": "0.11.0",
    "remotedev": "0.2.7"
  },
  "devDependencies": {
    "@babel/core": "7.4.5",
    "@babel/runtime": "7.4.5",
    "babel-core": "^6.26.3",
    "fable-splitter": "0.1.21",
    "metro-react-native-babel-preset": "0.54.1",
    "metro-with-symlinks": "1.3.0"
  }
}
